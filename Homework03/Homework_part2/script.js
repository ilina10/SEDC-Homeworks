//converting human years to dog's age with function
let humanAge = (parseInt = prompt("Enter your age, to convert to dog's age"));
function converting(age) {
  return age * 7; // 1 human year = 7 dogs year
}
console.log("Your dog is " + converting(humanAge) + "years old");
